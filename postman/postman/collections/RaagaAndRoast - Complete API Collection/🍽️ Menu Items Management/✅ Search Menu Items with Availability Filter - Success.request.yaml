$kind: http-request
url: "{{baseUrl}}/api/menu-items/search?query=coffee&availableOnly=true&page=0&size=20"
method: GET
headers:
  Authorization: Bearer {{customerToken}}
queryParams:
  query: coffee
  availableOnly: "true"
  page: "0"
  size: "20"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Response has search results with availability filter", function () {
          var jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property('content');
      });
    language: text/javascript
order: 18000
