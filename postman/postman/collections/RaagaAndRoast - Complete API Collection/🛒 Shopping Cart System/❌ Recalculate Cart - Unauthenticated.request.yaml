$kind: http-request
url: "{{baseUrl}}/api/customers/{{customerId}}/cart/recalculate"
method: POST
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 401", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 15000
