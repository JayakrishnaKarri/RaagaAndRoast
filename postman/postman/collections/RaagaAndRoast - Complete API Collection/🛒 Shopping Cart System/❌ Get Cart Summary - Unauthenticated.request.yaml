$kind: http-request
url: "{{baseUrl}}/api/customers/{{customerId}}/cart/summary"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 401", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 14000
