$kind: http-request
url: "{{baseUrl}}/api/customers/marketing-consent/true?page=0&size=20"
method: GET
headers:
  Authorization: Bearer {{managerToken}}
queryParams:
  page: "0"
  size: "20"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Response has marketing consent filtered customers", function () {
          var jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property('content');
      });
    language: text/javascript
order: 13000
