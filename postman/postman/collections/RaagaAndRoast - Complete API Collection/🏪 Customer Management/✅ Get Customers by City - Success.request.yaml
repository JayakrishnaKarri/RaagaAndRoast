$kind: http-request
url: "{{baseUrl}}/api/customers/city/New York?page=0&size=20"
method: GET
headers:
  Authorization: Bearer {{staffToken}}
queryParams:
  page: "0"
  size: "20"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });

      pm.test("Response has city filtered customers", function () {
          var jsonData = pm.response.json();
          pm.expect(jsonData).to.have.property('content');
      });
    language: text/javascript
order: 14000
