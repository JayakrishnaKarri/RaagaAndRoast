$kind: http-request
url: "{{baseUrl}}/api/addresses/search/city?city=New York"
method: GET
headers:
  Authorization: Bearer {{customerToken}}
queryParams:
  city: New York
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 403", function () {
          pm.response.to.have.status(403);
      });
    language: text/javascript
order: 38000
